import React from 'react'

export default function createBuild() {
  return (
    <div>
      {/* Table of contents:
          - header
          - build items w/optional god container
          - build stats w/optional god stats added
          - filtered items based on scenario */}
    </div>
  )
}
